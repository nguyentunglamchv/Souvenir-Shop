@model IEnumerable<giadinhthoxinh.Models.tblImportMaterial>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section naviheader{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
    </ul>
}

<section class="content-header p-2">
</section>

<section class="content ml-2 mr-2">
    <div class="card">
        <div class="card-header">
            <h1 class="card-title text-lg text-danger">Danh sách  chi tiết phiếu nhập nguyên liệu</h1>

            <div class="card-tools">
                @Html.ActionLink("thêm mới", "Create", null, new { @class = "btn btn-primary" })
            </div>
        </div>

        <div class="card-body table-responsive text-nowrap">
            <table class="table table-hover table-striped">
                <thead class="thead-dark">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.iQuatity)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.fPrice)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.tblImportOrder.sDeliver)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.tblMaterial.sMaterialName)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.iQuatity)
                            </td>
                            <td>
                                @*@Html.DisplayFor(modelItem => item.fPrice)*@
                                @string.Format("{0:#,0}", item.fPrice) vnđ

                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tblImportOrder.sDeliver)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tblMaterial.sMaterialName)
                            </td>
                            <td>
                                @Html.ActionLink("sửa", "Edit", new { id = item.PK_iImportMaterialID }, new { @class = "btn btn-primary btn-warning" }) |
                                @Html.ActionLink("chi tiết", "Details", new { id = item.PK_iImportMaterialID }, new { @class = "btn btn-primary btn-info" }) |
                                @Html.ActionLink("xóa", "Delete", new { id = item.PK_iImportMaterialID }, new { @class = "btn btn-primary btn-danger" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="card-footer">

        </div>
    </div>
</section>
